<h2>New culture</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_culture_path(@conn, :create, @user) %>

<%= link "Back", to: user_culture_path(@conn, :index, @user) %>
