<h2>Редагувати археологічну культуру</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_culture_path(@conn, :update, @user, @culture) %>

<%= link "Назад", to: user_culture_path(@conn, :index, @user) %>
