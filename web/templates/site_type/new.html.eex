<h2>New site type</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_site_type_path(@conn, :create, @user) %>

<%= link "Back", to: user_site_type_path(@conn, :index, @user) %>
