<h2>Новий тип пам'яток</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_site_type_path(@conn, :create, @user) %>

<%= link "Назад", to: user_site_type_path(@conn, :index, @user) %>
