<h2>Listing publications</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for publication <- @publications do %>
    <tr>
      <td><%= publication.name %></td>

      <td class="text-right">
        <%= link "Show", to: user_publication_path(@conn, :show, @user, publication), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_publication_path(@conn, :edit, @user, publication), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: user_publication_path(@conn, :delete, @user, publication), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New culture", to: user_publication_path(@conn, :new, @user) %>
