<h2>Listing conditions</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for condition <- @conditions do %>
    <tr>
      <td><%= condition.name %></td>

      <td class="text-right">
        <%= link "Show", to: user_condition_path(@conn, :show, @user, condition), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_condition_path(@conn, :edit, @user, condition), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: user_condition_path(@conn, :delete, @user, condition), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New condition", to: user_condition_path(@conn, :new, @user) %>
