<h2>Edit condition</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_condition_path(@conn, :update, @user, @condition) %>

<%= link "Back", to: user_condition_path(@conn, :index, @user) %>
